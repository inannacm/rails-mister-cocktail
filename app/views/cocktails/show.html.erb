<br>
<div class="container" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(https://source.unsplash.com/random)">
  <h1><%= @cocktail.name %></h1>
  <div class="cards">
    <p><% @cocktail.doses.each do |dose|%></p>
      <p><%= dose.ingredient.name%></p>
      <p><%= dose.description %></p>
      <p><%= link_to 'Edit ingredients', dose_path(dose),  method: :delete, class: "btn btn-danger" %></p>
    <% end %>
  </div>
</div>


<div class="container-2">
  <div class="row">
    <div class="col-sm-6 offset-sm-3">
      <h1>Add ingredients</h1>
      <%= simple_form_for [@cocktail, @dose] do |f| %>
      <%= f.input :description%>
      <%= f.association :ingredient %>

      <%= f.submit class: "btn btn-primary" %>
      <% end %>
      <br>
      <%= link_to 'Back to cocktail list', cocktails_path, class: "btn btn-info" %>
    </div>

  </div>
</div>



